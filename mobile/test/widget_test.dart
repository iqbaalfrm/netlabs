import 'package:flutter_test/flutter_test.dart';

// Test placeholder — akan diisi saat integrasi backend selesai
void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
